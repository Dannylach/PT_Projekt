namespace HandPaint
{
    public enum Mode
    {
        None,
        Line,
        Rectangle,
        Ellipse,
        Brush,
        Erase
    }

    public enum BackgroundMode
    {
        White,
        Camera
    }
}